# DEV PROFILE -- model authoring only. CUBEJS_DEV_MODE=true disables member-level
# access control, so masking is NOT enforced and verify.sh V1 fails here by
# design. Switch back before showing anyone anything:
#   CUBE_MODE=demo docker compose up -d --force-recreate cube
CUBEJS_DEV_MODE=true
